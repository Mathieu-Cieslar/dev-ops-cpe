
- name: Run Application container
  docker_container:
    name: tp-1-httpd-1
    image: mathieuc71/httpd-cpe:latest
    pull: yes
    networks:
      - name: proxy-network
    restart_policy: always
    state: started
